<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dicoding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyNotesApp"></activity>
        <activity android:name=".FormAddUpdateActivity" />
        <activity android:name=".Luas" />
        <activity android:name=".FormUserPreferenceActivity" />
        <activity android:name=".MySharedPreference" />
        <activity android:name=".MyRecycleView" />

        <service
            android:name=".MediaService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".sound_pool" />
        <activity android:name=".Peniympanan_lokal" />
        <activity android:name=".sqlite" />
        <activity android:name=".MoveActivityObject" />
        <activity android:name=".MoveWithData" />
        <activity android:name=".MoveActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>